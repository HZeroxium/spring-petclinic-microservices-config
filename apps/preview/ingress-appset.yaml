# apps/preview/ingress-appset.yaml
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: preview-ingress
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: 'https://github.com/HZeroxium/spring-petclinic-microservices-config.git'
        revision: HEAD
        directories:
          - path: 'ingress/preview/preview-*'
  template:
    metadata:
      name: 'ingress-{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: 'https://github.com/HZeroxium/spring-petclinic-microservices-config'
        targetRevision: HEAD
        path: 'ingress/preview/{{path.basename}}'  
        directory:
          recurse: true                        
      destination:
        server: https://kubernetes.default.svc
        namespace: 'preview-{{path.basename}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true

